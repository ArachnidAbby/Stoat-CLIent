[project]
name = "stoat-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "comprehensiveconfig>=1.1.2",
    "pyperclip>=1.11.0",
    "stoat-py>=1.2.1",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
]
